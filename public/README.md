# Public Folder

Place your custom **`favicon.png`** file directly in this folder. 

Once placed:
- The path to the icon will resolve to `/favicon.png`.
- The `index.html` file is already configured to read it: `<link rel="icon" type="image/png" href="/favicon.png" />`.
